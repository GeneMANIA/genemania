<%@ taglib uri="http://java.sun.com/jsp/jstl/core" prefix="c" %><%@ taglib uri="http://java.sun.com/jsp/jstl/fmt" prefix="fmt" %><%@ taglib uri="http://www.springframework.org/tags" prefix="spring"%><%@ taglib prefix="fn" uri="http://java.sun.com/jsp/jstl/functions" %><%@ taglib uri="http://java.sun.com/jsp/jstl/fmt" prefix="fmt" %><%@ page contentType="text/plain" %><% response.setHeader("Content-Disposition", "attachment"); response.setHeader("charset", "UTF-8"); %>Group	Name	Weight	Reference	PubMed	Source	Note	Tags<c:forEach items="${results.resultNetworkGroups}" var="rNetworkGroup">
<c:forEach items="${rNetworkGroup.resultNetworks}" varStatus="iterationnet" var="rNetwork"><c:set var="network" value="${rNetwork.network}" />${rNetworkGroup.networkGroup.name}	${rNetwork.network.name}	<fmt:formatNumber value="${rNetwork.weight * 100}" minFractionDigits="2" maxFractionDigits="2" /> %	<c:if test="${ !empty network.metadata.authors }">${network.metadata.title}.  ${fn:substringBefore(network.metadata.authors, ",")} <spring:message code='networks_tab.et_al'/>.  (${network.metadata.yearPublished}).  ${network.metadata.publicationName}.</c:if>	${network.metadata.url}	<c:if test="${ !empty network.metadata.processingDescription }">${network.metadata.processingDescription} </c:if><c:if test="${ !empty network.metadata.interactionCount }"><spring:message code='networks_tab.with'/> <fmt:formatNumber pattern="###,###,###,###" value="${network.metadata.interactionCount}"/> <spring:message code='networks_tab.interactions'/> </c:if><c:if test="${ !empty network.metadata.source && network.id > 0 }"><spring:message code='networks_tab.from'/> <spring:message code='network_source.${fn:toUpperCase(network.metadata.source)}'/></c:if><c:if test="${ !empty network.metadata.comment && network.id < 0 }"><spring:message code='networks_tab.from'/> ${network.metadata.comment}</c:if>	<c:forEach items="${network.tags}" var="tag" varStatus="iteration">${fn:toLowerCase(tag.name)}<c:if test="${!iteration.last}"> </c:if></c:forEach><c:if test="${!iterationnet.last}">
</c:if></c:forEach></c:forEach>
<c:forEach items="${results.resultAttributeGroups}" var="rAttrGr" varStatus="iteration"><c:forEach items="${rAttrGr.resultAttributes}" varStatus="iterationnet" var="rAttr"><c:set var="attr" value="${rAttr.attribute}" />${rAttrGr.attributeGroup.name}	${rAttr.attribute.name}	<fmt:formatNumber value="${rAttr.weight * 100}" minFractionDigits="2" maxFractionDigits="2" /> %	<c:if test="${!iterationnet.last}">
</c:if></c:forEach><c:if test="${!iteration.last}">
</c:if></c:forEach>